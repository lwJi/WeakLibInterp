# TODO — WeakLibInterp build plan

Current state: mature and functionally spec-complete. All 13 research slices this plan run confirm every leaf contract — EOS evaluate/differentiate/inversion, EmAb/Iso, NES/Pair, Brem, the differentiate kernels, all six HDF5 readers, MPI root-read/broadcast, the device interface and `ParallelFor` wrappers, the Cactus thorn, the build/CI matrix, and the tolerance framework — traces to implemented, spec-cited code with green tests. No interpolation math, kernel, reader, or launcher is missing anywhere. What remains is a short tail: real-table verification-coverage cells the specs literally ask for but the production-table suite only spot-checks (Tier 11), then comment hygiene and small consolidation/polish (Tier 12). The one genuine spec silence found (`/EC_table` disposition) was repaired by a spec-author this plan run — resolved out-of-scope, no build item. Do not manufacture work — the "do NOT build" list in Decisions is load-bearing.

## Standing facts every item inherits

- **Value type pinned to `double`** on the entire correctness-bearing path (`src/core/wli_real.H`, `wli::Real`), independent of `amrex::Real`. Never let `amrex::Real` leak into the math.
- **Every new CMake knob is `WLI_`-prefixed and defaults to current behavior** — the default `cmake -S . -B build` tree must stay byte-unchanged (32 tests as of this plan run), exactly as `WLI_AMREX_MPI`/`WLI_AMREX_PRECISION`/`WLI_GPU_BACKEND`/`WLI_BUILD_TESTS`/`WLI_AMREX_INSTALL_DIR` already do.
- **Tolerance tiers** (`src/core/wli_compare.H`): parity `rtol=1e-12, atol=1e-30`; relaxed `1e-10`; machine `~1e-14`; boundary/NaN cells use exact predicates (`std::isfinite`/`std::isnan`/`==`), never `is_close`. `fortran-parity-and-tolerances.md:109` requires any relaxed-tier use to carry a stated rationale in the leaf spec itself.
- **Spec validation must stay green:** `AMREX_ROOT=../amrex tools/q bash specs/tools/validate_specs.sh` — 12 registered specs; confirmed green after this run's `regression-suite-design.md` EC_table edit.
- **Sandbox limits:** `WL_TABLES_ROOT` unset by default (all production-table cells SKIP 77) — this host's live copy is `/Users/liwei/Datas/wl_tables/use_for_production`; run `tools/test.sh --tables /Users/liwei/Datas/wl_tables/use_for_production` before committing any `test_production_tables.cpp` change (a SKIPped cell proves nothing). `build-mpi/` and the GPU trees are CI-verified, not local; NO Cactus/ET checkout exists (only `../amrex`, `../weaklib`, `../thornado`) — Cactus Verification #5 stays manual (`cactus-integration.md:79`).
- Cap parallel builds at `-j4` (uncapped AMReX builds OOM this host). After adding a new test target to `test/CMakeLists.txt`, re-run the configure step before building. Known recurring host quirk: `build/` CMakeCache can go stale pinned to /opt/homebrew mpicxx — recovery is wipe `build/` + reconfigure with `CXX=g++ CC=gcc`.

## Tier 11 — Real-table verification coverage

- [x] Real-table boundary-extrapolation cells for EmAb and Iso
  - spec: opacity-emab-iso.md (§:140 — all checks "run against both synthetic in-suite tables and the real reference tables"; boundary policy §:94-99 — clamp bracket index, unclamped delta ⇒ extrapolate, stay finite); tier per fortran-parity-and-tolerances.md:101,113-119 (exact/no-tolerance)
  - tests: extend `run_emab` (`test/test_production_tables.cpp:280-355`, currently has NO boundary cell) with below-min and above-max queries on each of E, rho, T, Ye (one axis perturbed at a time, others at interior nodes), asserting `std::isfinite` — no `is_close`, no tolerance — mirroring `test/test_emab_point.cpp:154-199`; extend `run_iso` (`:357-491`) the same way for rho, T, Ye (below-edge E already covered at `:394-399`), mirroring `test/test_iso_point.cpp:187-227`. Cells stay `WL_TABLES_ROOT`-gated (SKIP 77); verify with the `--tables` run before committing.
  - notes: DONE 2026-07-28. `run_emab` gained 8 checks ({below,above}×{E,rho,T,Ye}) and `run_iso`'s below-E cell was extended to the full 8 — quarter-cell probes in log space (raw for Ye), bare `std::isfinite`, exact tier; the correct spec anchor is opacity-emab-iso.md:120-126 (the §:94-99 refs above had drifted). Verified with `tools/test.sh --tables …` — 89/89 passed incl. `production_tables_np2/np4`, all 16 new `ok:` lines printed; default suite unchanged (SKIP 77 without tables). Iso cells use the pinned `iMom`/`OS`/`tbl` scope, not the FD block's `cM`/`OSfd`.

- [x] Real-table moment-slice independence for Iso
  - spec: opacity-emab-iso.md (§:140 real-table requirement; §:144 moment-slice independence; §:90-91 `IsoOffset` 2D species-major/moment-minor offsets — implementation `src/opacity/wli_opacity_emab_iso.H:229-232`)
  - tests: in `run_iso`, evaluate `IsoInterpolateSingleVariable5DPoint` at the same (E, rho, T, Ye) for two distinct `iMom` (e.g. 0 and nMom-1) and assert the results differ (exact inequality, not a tolerance check). Guard against vacuity: first confirm the raw node values (and per-moment offset) backing the two slices are not identical; if they coincide on the real table, try another moment pair rather than failing. Same `WL_TABLES_ROOT`/SKIP-77 gate; verify with the `--tables` run before committing.
  - notes: DONE 2026-07-28. New cell at `test/test_production_tables.cpp:439-497` (after the node-identity check, before the boundary block): per-moment `IsoOffset` locals (never the pinned file-level `OS`), pair selection prefers `(0, nMom-1)` with a scan fallback (no literal indices), vacuity guard is exact `!=` on raw node values OR offsets; if no pair is usable it reports via `check(mA >= 0, ...)` (FD-block guard shape) — a fully-degenerate table is treated as a loader-defect signal, not a soft pass. Verified with `tools/test.sh --tables …` — 89/89 incl. np2/np4; production Iso table has `nMoments == 2` so the pair is (0,1); default suite unchanged (SKIP 77). Mutation-checked: feeding the same `(m, OS)` to both calls flips the cell to FAIL. Optional machine-tier strengthening (`!is_close` at `rtol_machine`) was left out; exact `!=` is the assertion.

- [x] Real-table exercise of the NES detailed-balance and Pair crossing-symmetry fills
  - spec: opacity-nes-pair.md (§:148-156 NES detailed balance, factor `exp((E[iE]-E[iEp])/T)`; §:158-174 Pair crossing symmetry — pure relabeling, no Boltzmann factor, diagonal NOT a fixed point; verification items 4-5 §:200-206)
  - tests: in `run_nespair` (`test/test_production_tables.cpp:493-615`): at an upper-triangle index (iEp > iE) on the live NES table, assert `NESDetailedBalanceFillPoint` (`src/opacity/wli_opacity_nes_pair.H:192-204`) equals the independently recomputed `primitive(swapped) * exp((E[iE]-E[iEp])/T)` at `wli::rtol_parity`/`wli::atol_default`, plus the diagonal identity (iEp == iE ⇒ factor 1); on the live Pair table, assert `PairCrossingSymmetryFillPoint` (`:234-244`) equals the swapped-index primitive evaluation exactly (`==`, pure relabeling). Same SKIP-77 gate; verify with the `--tables` run before committing.
  - notes: DONE 2026-07-28. New closure block at `test/test_production_tables.cpp:759-963` inside `run_nespair`, after the FD block, gated `if (energyOk && nes) {…} else if (energyOk) {…}`. Key facts: `t.nPoints` is Fortran-ordered so `nEp == nE == 40` and the pinned midpoint is on the diagonal — upper-triangle cells run their own `iEp > iE` scan; unit trap handled with test-local `kBoltzmannMeVPerK = 8.61733e-11` (Kelvin axis → the fill leaf's k_B·T-in-MeV `T`), mutation-checked (k_B=1.0 flips the scan guard to FAIL). NES took candidate `(iEp,iE,kernel)=(21,20,2)`; Pair's scan rejects the mid-table constant floor and lands on `(1,0,0)` with `kernelSwapped = k^1` and its own `IsoOffset` — exact `==` held on the live Pair table, no is_close fallback needed. Verified both `tools/test.sh` (SKIP-77 unchanged) and `tools/test.sh --tables …` — 89/89 each incl. np2/np4.

## Tier 12 — Hygiene & consolidation

- [ ] Fix the stale "no HDF5 loader exists yet" comment in `test/test_emab_point.cpp`
  - spec: regression-suite-design.md (comment hygiene; no behavior change)
  - tests: none — comment-only; existing suite stays green.
  - notes: Found during the Tier 10 four-file comment fix (2026-07-07) but explicitly out of that item's scope. Same fix pattern: rewrite only the stale DEFERRED tail to forward-reference the real-table EmAb coverage in `test_production_tables.cpp` (via `read_emab_table` in `src/io/wli_io_opacity.{H,cpp}`), preserving the accurate "hand-rolled harness / synthetic table / no amrex::Initialize" leading clause; confirm the exact line range by grep before editing. This plan run pinpointed it: `test_emab_point.cpp:32-34` ("Real-.h5 production parity is deferred … no HDF5 loader exists yet" — false; `run_emab` at `test_production_tables.cpp:280-281`, registered `:821`); keep the trailing "moment-slice independence is Iso-only" note at the end of `:34` (still accurate). Do this AFTER the Tier 11 EmAb boundary item if both land, so the referenced coverage description is final. Optional same-cycle companion (not its own item, not independently testable): `src/core/wli.cpp:3-4`'s "Interpolator entry points are added by later increments" framing is stale — entry points landed as header-only kernels in `src/eos/`/`src/opacity/`.

- [ ] Consolidate the hand-built `EosInversionBounds` boilerplate into one test-side helper
  - spec: eos-inversion.md (§:163,169 — bounds representation deliberately free, so NO `src/` builder; CLAUDE.md anti-duplication discipline is the only pressure)
  - tests: no new assertions; backpressure is that all inversion cells stay bit-identical green — `eos_inversion_dey`, `eos_inversion_family`, `eos_inversion_describe`, `production_tables` (including the `--tables` run), and the MPI 1/2/4-rank fan-out (`test/CMakeLists.txt:59-81,438-440`).
  - notes: The same min/max sub-table scan + axis-extent construction is hand-duplicated at `test/test_eos_inversion_dey.cpp:96-113`, `test/test_eos_inversion_family.cpp:97-112` and `:231-246`, and `test/test_production_tables.cpp:236-249`. Prior plan marked this flag-only, "touch only if an increment is already in the area" — Tier 11 touches `test_production_tables.cpp`, so this run IS the window. Shape: one test-only header alongside `test/h5ls_snapshot.H`/`test/wli_rank_digest.H` (the existing test-fixture-header convention) building `EosInversionBounds` (`src/eos/wli_eos_inversion.H:60-65`) from a loaded sub-table; convert all four call sites; do NOT add it to `src/`. Explicitly out of scope: the `Alpha_Brem` literal's third copy (`test_brem_point.cpp:195`, `test_brem_diff_point.cpp:207`, `test_production_tables.cpp:731`) — per-TU fixture duplication is the project's stated convention.

- [ ] Emit an explicit failure diagnostic from the Cactus thorn's build half
  - spec: cactus-integration.md (§:69 — find-or-build "never fails silently" in BOTH directions; §:82-89 grants implementation freedom on message text, so this is polish, not a violation)
  - tests: extend `specs/tools/validate_thorn.sh` (checks at `:78-127`) with a structural check that `build.sh` emits an explicit diagnostic on a non-zero CMake/make exit; gate is manual, not ctest: `tools/q bash specs/tools/validate_thorn.sh`. In-Cactus acceptance (`tools/cactus-build.sh`) is host-gated and unaffected (no ET checkout in this sandbox).
  - notes: The detect-mode bad-prefix half already emits a `BEGIN ERROR` block (`cactus/thorns/WeakLibInterp/src/detect.sh:57-68`); the build-mode half relies solely on `set -e` (`build.sh:14`) plus make's exit propagation through `make.code.deps:27` — hard failure holds, but no "could neither find nor build" message is emitted. Lowest priority in the plan.

## Decisions & non-blocking spec items

These are decisions/notes, NOT build increments — the responsible increment resolves each once and reuses it.

- **`/EC_table` — RESOLVED this plan run (spec-author dispatched):** disposition written into `specs/regression-suite-design.md` — `/EC_table` stays OFF the coverage matrix (table-integrity/rank-digest only), with a note after the 8-row closure paragraph plus a matching "Out of scope" bullet. Deciding evidence: no `src/opacity/` kernel and no routine under `weaklib/Distributions/Library/` consumes the EC data, so there is no `_Point` oracle to assert against. `validate_specs.sh` confirmed green after the edit. No build item; do not add a 9th coverage row.
- **Real-table coverage scope judgment:** Tier 11 items 1-2 are spec-mandated (`opacity-emab-iso.md:140` says all checks run against the real tables too); item 3 is elective (NES/Pair spec does not extend items 4-5 to real tables). Prior Tier-9 precedent treated this class as build-worthy — that is why they lead this plan.
- **Coverage-matrix registration of the derivative entry points — decided: not registered.** Covered by dedicated `test_*_diff_point.cpp` targets plus the real-table FD cells. Revisit only if `regression-suite-design.md` gains an exhaustiveness assertion over entry points. `check_coverage_matrix_closure` (`specs/tools/validate_specs.sh:438-471`) asserts names appear in the spec, not that `test_coverage_matrix.cpp` calls them — accepted under implementation freedom (`regression-suite-design.md:117-121`).
- **`BremPointDeriv.dDrho` is intentionally `0`** from the summed wrapper; the base-density chain rule is consumer-side assembly (`opacity-brem.md:35`, `opacity-differentiate.md:112,169`; comment at `src/opacity/wli_opacity_brem.H:232-238`). Becomes a task only if a consumer spec appears.
- **No `HostEosTable` → `ResidentTable` bridge builder exists in `src/`** — `TableMeta::kind`/`OS` are a caller-set convention populated only by tests (`src/core/wli_table.H:48-59`, documented as intentional). Not a gap today; it is the natural seam if IO→device integration ever becomes in-scope.
- **Node-identity machine tier held on the pinned live tables** (reconciled 2026-07-07). Re-confirm empirically if `specs/fixtures/tables.provenance` ever changes.
- **Dropped from the prior plan as stale:** the claim that `.gitignore` gained `build-installed/` (it did not; no installed preset or tree exists) and the `AMREX_ENABLE_MPI` "unverifiable variable name" worry (the thorn's `build.sh` forwards no MPI setting at all, by design per `cactus-integration.md:63`, and `validate_thorn.sh` actively forbids that variable).
- **Resolved by spec — do NOT build:** chunked-broadcast IMPLEMENTATION (`table-format-and-io.md:171`; every pinned dataset ~2 orders below 2^31−1; deferral comment already at `src/io/wli_io_bcast_detail.H:108-119`); any external Fortran numeric fixture/oracle (self-contained scheme by design, `fortran-parity-and-tolerances.md:135-142`); Cactus Verification #5 CUDA/HIP leg, official ET packaging, HDF5 C-API port, consumer thorn, ctest-inside-Cactus (`cactus-integration.md:15-19,91-98`); no combined MPI×GPU CI job; no parallel HDF5.

## Discovered since last plan
