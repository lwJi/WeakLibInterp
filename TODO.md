# TODO — WeakLibInterp build plan

Current state: mature and largely spec-complete. All eight device entry-point families (evaluate AND differentiate), the 20 host `ParallelFor` array wrappers, the shared math core, all six HDF5 readers, the MPI root-read/broadcast path, the Cactus thorn + installed-AMReX consumption mode, and the full CI matrix are implemented, spec-traced, and green — the prior plan's Tiers 7–8 are fully drained. Nine of thirteen research slices this plan run found zero functional gaps; no new interpolation math or kernels are needed anywhere. What remains: closing literal spec-Verification coverage holes and one tolerance-tier violation (Tier 9), then de-duplication and hygiene (Tier 10). A spec-author was dispatched this plan run to repair stale "still-absent"/"dead" prose in `specs/opacity-differentiate.md` (descriptive language only — no normative change).

## Standing facts every item inherits

- **Value type pinned to `double`** on the entire correctness-bearing path (`src/core/wli_real.H`, `wli::Real`), independent of `amrex::Real`. Never let `amrex::Real` leak into the math.
- **Every new CMake knob is `WLI_`-prefixed and defaults to current behavior** — the default `cmake -S . -B build` tree must stay byte-unchanged (30 tests as of the Tier 8 completion), exactly as `WLI_AMREX_MPI`/`WLI_AMREX_PRECISION`/`WLI_GPU_BACKEND`/`WLI_BUILD_TESTS`/`WLI_AMREX_INSTALL_DIR` already do.
- **Tolerance tiers** (`src/core/wli_compare.H`): parity `rtol=1e-12, atol=1e-30`; relaxed `1e-10`; machine `~1e-14`; rank-consistency cells bitwise-exact. `fortran-parity-and-tolerances.md:109` requires any relaxed-tier use to carry a stated rationale in the leaf spec itself.
- **Spec validation must stay green:** `AMREX_ROOT=../amrex tools/q bash specs/tools/validate_specs.sh` — 12 registered specs; per-spec section/tolerance/source-path gates include `opacity-differentiate.md`.
- **Sandbox limits:** `WL_TABLES_ROOT` unset by default (all production-table cells SKIP 77) — this host's live copy is `/Users/liwei/Datas/wl_tables/use_for_production`; run with it before committing any `test_production_tables.cpp` change. `build-mpi/` and the GPU trees are CI-verified, not local; NO Cactus/ET checkout exists (only `../amrex`, `../weaklib`, `../thornado`) — Cactus Verification #5 stays manual (`cactus-integration.md:79`).
- Cap parallel builds at `-j4` (uncapped AMReX builds OOM this host). After adding a new test target to `test/CMakeLists.txt`, re-run the configure step before building.

## Tier 9 — Spec-verification coverage & parity reconciliation

- [x] Exercise the `/EmAb_CorrectedAbsorption` legacy fallback
  - spec: table-format-and-io.md (§:182, Verification #4) — explicitly calls for a synthetic/legacy fixture lacking `/EmAb`
  - tests: a synthetic EmAb fixture written under `/EmAb_CorrectedAbsorption` only (no `/EmAb`) reads successfully via `read_emab_table`, asserts `t.usedLegacyGroup == true`, and read-back values match what was written. Always-on serial test in the default tree (spec does not gate #4 on MPI).
  - notes: DONE (2026-07-07, build loop): new `test/test_emab_legacy_fallback.cpp` (always-on serial, registered at `test/CMakeLists.txt:269-289`, no SKIP_RETURN_CODE) writes the fixture under `wli::io::schema::emab::kLegacyGroup` only, asserts `usedLegacyGroup==true`, `nOpacities==2`, `offset=={1.0,2.0}`, byte-identical (`==`) value read-back for both species, speciesNames, `hasEmAbParameters==false`, `hasECTable==false`. Fixture-writer helpers are per-TU duplicates from `test_rank_consistency.cpp` by design (no shared writer header; `src/io/` stays read-only). Default tree is now 31 tests, all green. Iteration quirk: the pre-existing `build/` CMakeCache was stale from a prior host (pointed at /opt/homebrew mpicxx) — wiped and reconfigured with explicit `-DCMAKE_CXX_COMPILER=g++ -DCMAKE_C_COMPILER=gcc`; one-off recovery, not a durable command change.

- [x] Round-trip layout check against a real table (dataset value vs flat-offset formula)
  - spec: table-format-and-io.md (§:180, Verification #2) — address an element by the flat-offset formula and confirm it matches the independently-addressed value at the corresponding C-order index
  - tests: a `WL_TABLES_ROOT`-guarded cell (SKIP 77, mirroring `test_production_tables.cpp` discipline) that computes a flat offset via `wli::flat_index`/the documented column-major formula for a real dataset element and asserts exact equality (`==`, no tolerance — this is a layout check, not interpolation) against a second, independent raw HDF5 read at the corresponding C-order index. Confirm with live tables before committing.
  - notes: DONE (2026-07-07, build loop): new standalone `test/test_layout_roundtrip.cpp` (registered `test/CMakeLists.txt:291-315` with SKIP_RETURN_CODE 77, plus MPI fan-out at `:431`). Path A: `read_eos_table` + `wli::flat_index<3>({iRho,iT,iYe},{nRho,nT,nYe})` on `dv[0].values`; Path B: independent `H5::H5File` RDONLY open + 1-element `selectHyperslab` at reversed C-order start `{iYe,iT,iRho}` on `/DependentVariables/<dv[0].name>`; asserts bit-exact `==` on the log-stored double (no `recover`). Verified live (dv[0]="Pressure", interior tuple iRho=100,iT=40,iYe=15); default tree now 32 tests, all green. Recurring host quirk: `build/` CMakeCache was AGAIN stale (pinned to /opt/homebrew mpicxx) — second consecutive iteration; recovery is wipe `build/` + reconfigure with `CXX=g++ CC=gcc`; now recorded in CLAUDE.md Build & run.

- [x] Reconcile production-table node-identity tolerance tier (spec vs test divergence)
  - spec: fortran-parity-and-tolerances.md (§:100-101,109,139) + leaf specs eos-interpolation.md:115, opacity-emab-iso.md:143, opacity-nes-pair.md:201, opacity-brem.md:205 — all state node identity = machine-precision tier, verified on the real table too
  - tests: the reconciled `test_production_tables.cpp` node-identity cells pass at the chosen tier against a real `WL_TABLES_ROOT` table (currently unset in this sandbox → cells SKIP 77; confirm empirically before committing the tier choice). Test-only OR spec-only edit; no `src/` change.
  - notes: DONE (2026-07-07, build loop): option (a) — tightened all 5 call sites (`test_production_tables.cpp:126,237,323,468,581`; `:468` is the shared `run_nespair` runner covering both NES and Pair) from `wli::rtol_relaxed` to `wli::rtol_machine`. Empirically confirmed FIRST against the live tables (`WL_TABLES_ROOT=/Users/liwei/Datas/wl_tables/use_for_production`): all six node-identity cells pass at 1e-14. The flagged EOS residual risk (raw physical coords through `GetIndexAndDeltaLog`'s internal log10+floor possibly landing in the adjacent cell at interior nodes) did NOT materialize on real data. No spec carve-out warranted — the Decisions-section "Node-identity carve-out" contingency is moot. Test-only change; default tree stays 32/32 green. Host quirk recurred a third time: stale `build/` CMakeCache pinned to /opt/homebrew mpicxx, recovered per the CLAUDE.md-documented wipe+`CXX=g++ CC=gcc` reconfigure.

- [x] EOS differentiate kernel real-table FD cross-check
  - spec: eos-interpolation.md (§:116) — "Cross-check against a tight central finite-difference at the relaxed tier on the real table"
  - tests: central-FD-vs-analytic-derivative agreement at `rtol_relaxed` on `wl-EOS-SFHo-15-25-50.h5`, under the same `WL_TABLES_ROOT` SKIP-77 guard. Test-only, no `src/` change. Confirm with live tables before committing.
  - notes: DONE (2026-07-07, build loop): new braced block in `run_eos()` (`test/test_production_tables.cpp:140-208`, after the NaN check, before the inversion section). Reuses the file's FD-conditioning discipline: scans interior cells [1,n-2) on rho/T/Ye for per-axis stored-log slopes in `[kFdSlopeMin,kFdSlopeMax]` (LOG axes rho/T normalize by `kLn10*log10(node ratio)`, LINEAR Ye by `*Yc/(node gap)`), asserts such a cell exists, then cross-checks `EosInterpolateDifferentiateSingleVariable3DPoint`'s dDrho/dDT/dDY against a 4th-order central FD at the cell center (`sqrt` log-midpoints for rho/T, arithmetic for Ye; relative step 1e-3) at `rtol_relaxed`/`atol_default`. Key difference from the opacity FD cells: EOS axes are RAW physical (the kernel logs rho/T internally), so `evalP` perturbs and passes raw `(D,T,Y)` with no `std::log10` wrapping. Verified live FIRST (`WL_TABLES_ROOT=/Users/liwei/Datas/wl_tables/use_for_production`): all three FD checks + the conditioned-cell guard pass at 1e-10; default tree stays 32/32 (SKIP branch intact). Relaxed-tier rationale already lives in the leaf spec (`eos-interpolation.md:91-92`), so no spec edit was needed. No CMake change (existing target). No host quirks this iteration — the stale-CMakeCache recovery in CLAUDE.md was not needed.

- [x] Extend the EOS rank digest to all 15 `HostDVIndices`
  - spec: table-format-and-io.md (§:183) — digest must fold "the extents, offsets, and names" of every loaded array
  - tests: the existing `test_rank_consistency.cpp` load/corrupt modes now catch a corruption injected into any of the previously-unfolded 12 indices (extend the corrupt positive-control to cover one). Test-only (`test/wli_rank_digest.H` edit).
  - notes: DONE (2026-07-07, build loop): `digest(HostEosTable)` (`test/wli_rank_digest.H`) now folds all 15 `HostDVIndices` in declaration order == `bcast_eos_table` pack order (`src/io/wli_io_eos.cpp:167-182`, idx[0]=iPressure … idx[14]=iGamma1 — confirmed identical). Corrupt positive-control (`test_rank_consistency.cpp:test_corrupt`) extended as a two-stage check so the new fold is demonstrated INDEPENDENTLY: Stage 1 perturbs previously-unfolded `iInternalEnergyDensity` alone on the victim rank, asserts digest divergence, restores; Stage 2 keeps the original `dv[0].values[0]` corruption + its check. No CMake/src edits (executable + all 6 MPI / 3 serial registrations pre-existed). Local acceptance structural by design: default tree 32/32 green, rank_consistency cells SKIP 77 (no MPI); runtime verification of the new positive control is CI-side in `build-mpi/`. Reuse sweep confirmed no golden digest constants anywhere (all digest use is relative cross-rank equality), and `test_mpi_root_bcast.cpp` (the header's only other includer) is unaffected. No host quirks; brief's plan of co-mingling both mutations in one check was corrected to the staged design at dispatch time.

- [x] Brem summed-decomposition coverage on the real table
  - spec: opacity-brem.md (§:202) — Verification #4-6 (decomposition closure, weight-order sensitivity, both triangles) must run against "both synthetic in-suite tables and the real reference table"
  - tests: extend `run_brem` (`test/test_production_tables.cpp:549-639`) to call `BremInterpolateSingleVariable2D2DAlignedSummedPoint` with 3 distinct effective densities (from a plausible `(xp, xn)` pair) against the live `.h5`, mirroring `test_brem_point.cpp:192-266` checks 4/5/6. Same `WL_TABLES_ROOT`/SKIP-77 gating; confirm with live tables before committing.
  - notes: DONE (2026-07-07, build loop): new block in `run_brem()` (`test/test_production_tables.cpp:707-780`, after the FD block) — local `K`/`brem` lambdas, in-grid query at mid-table cell centers with `xp=0.3, xn=0.5` (all three effective densities stay inside the 185-node rho grid), `Alpha_Brem={1,1,28/3}` inline. #4 closure + three-terms-distinct and #5 weight-permutation at parity tier; #6 asserts each of `(a=nEp/3, b=2*nEp/3)` and its transpose equals its OWN independent decomposition. Spec-consistent deviation from the one-liner: the pinned production table's `S_sigma` is bit-for-bit transpose-symmetric in the two neutrino energies (0 differing pairs across all 11,688,300 upper-triangle cells — bremsstrahlung is physically symmetric in the energy pair), so the "transposed outputs differ" assertion is impossible on real data; spec §209 conditions that assertion on "a table not symmetric under transposing" (satisfied only by the synthetic table, already covered in `test_brem_point.cpp:238-266`) — no spec repair needed, symmetry documented inline at `test_production_tables.cpp:~772`. Verified live FIRST (`WL_TABLES_ROOT=/Users/liwei/Datas/wl_tables/use_for_production`): all five new cells pass; default tree stays 32/32 green (SKIP path intact). Test-only, no src/CMake change. Third copy of the Alpha_Brem/LogD3 construction pattern noted (non-blocking). No host quirks this iteration.

## Tier 10 — Hygiene & de-duplication

- [x] De-duplicate the EOS reader-schema parser into `h5ls_snapshot.H`
  - spec: regression-suite-design.md + CLAUDE.md anti-duplication discipline (consolidated implementations over ad-hoc copies)
  - tests: the existing `test_eos_reader_schema` target passes unchanged after switching to `#include "h5ls_snapshot.H"` and deleting the inline copy (it already validates `schema::` expectations against `specs/fixtures/*.h5ls`).
  - notes: DONE (2026-07-07, build loop): deleted the full inline duplicate (former `test_eos_reader_schema.cpp:24-143` anonymous-namespace body) and switched to `#include "h5ls_snapshot.H"` + using-declarations for `h5ls::{Entry,check,expect_dataset,expect_group,expect_offsets,load_snapshot}`, mirroring `test_opacity_reader_schema.cpp:19-28`; exit-code checks now read `h5ls::g_failures`. Bonus consolidation the shared header enabled: the hand-rolled `/DependentVariables/Offsets` rank-1/length block folded into one `expect_offsets(m, ".../Offsets", {S::kNVariables})` (behavior-identical). No src/specs/CMake change; incremental build, no reconfigure needed (and no stale-CMakeCache quirk this iteration). Default tree 32/32 green (7 guarded cells SKIP as expected). `h5ls_snapshot.H` is now genuinely the single parser, as its header comment claims.

- [x] `.gitignore`: add the missing variant build trees
  - spec: none needed — repo hygiene; `docs/BUILD.md:13-20` lists six variant trees, each needing "its own gitignored tree"
  - tests: none — `git status` stays clean after a hypothetical local variant configure; existing suite unaffected.
  - notes: DONE (2026-07-07, build loop): appended `build-cuda/`, `build-rocm/`, `build-installed/`, `build-guard/` to the existing block (now `.gitignore:8-15`), one `<name>/` per line, no comments. `build-guard/` included (not just "optionally") because `ci.yml:345` creates it INSIDE the repo checkout (`cmake -B WeakLibInterp/build-guard`), so a local rehearsal of that step would dirty the tree. Verified: `git check-ignore -v` resolves all four to the new rules; `git status --porcelain` showed only `.gitignore` modified. CI-side scratch dirs `amrex-build`/`amrex-prefix`/`wli-prefix`/`scratch` deliberately NOT added — they live at `$GITHUB_WORKSPACE`, outside the repo tree. No host quirks; no rebuild needed.

- [x] Fix stale "no HDF5 reader / DEFERRED" comments in four test headers
  - spec: regression-suite-design.md (comment hygiene; no behavior change)
  - tests: none — comment-only; existing suite stays green.
  - notes: DONE (2026-07-07, build loop): rewrote only the stale "…is DEFERRED (no HDF5 reader exists yet)" tail in each header block — `test_nes_pair_point.cpp:28-31` and `test_brem_point.cpp:39-42` now forward-reference `run_nespair`/`run_brem` in `test_production_tables.cpp` (via the `src/io/wli_io_opacity.{H,cpp}` reader) keeping their original `rtol=1e-12` qualifiers (Brem also keeps the `wl-Op-SFHo-15-25-50-E40-Brem.h5` name); `test_nes_detailed_balance.cpp:20-22` and `test_pair_crossing_symmetry.cpp:29-31` point at `run_nespair` (Pair pass for the latter) with no rtol added (none in the original). The still-accurate leading clause ("Hand-rolled harness … synthetic 5D table only (no HDF5), no amrex::Initialize") left verbatim in all four. Forward-referenced symbols verified: `run_nespair` at `test_production_tables.cpp:493`, `run_brem` at `:617`. Default tree 32/32 green (7 guarded SKIPs). Discovered during scoping: `test_emab_point.cpp` carries a same-class stale comment — recorded in Discovered, NOT fixed here (one thing per loop). No host quirks.

- [x] Document the chunked-broadcast deferral at the `bcast_vector` cast site
  - spec: table-format-and-io.md (§:171, requirement 7 — datasets exceeding the MPI int limit are broadcast in chunks)
  - tests: none — comment-only; suite stays green.
  - notes: DONE (2026-07-07, build loop): extended the existing `bcast_vector` block comment (`src/io/wli_io_bcast_detail.H:108-119`) — one shared comment covering both overloads (double + int share the identical cast reasoning): names the size_t→int narrowing and the 2^31−1 MPI int count limit, cites `spec:171` requirement 7 (bare-line form matching the file's house style), states the deferral is deliberate/spec-acknowledged, gives the concrete bound (largest pinned dataset = NES/Pair `Kernels {120,81,4,40,40}` = 62,208,000 doubles ≈6.2e7, two orders below ~2.1e9) with `specs/fixtures/tables.provenance` as the provenance anchor, and sketches where the chunk loop would go if ever needed. Comment-only diff; default tree rebuilt and stayed 32/32 green (guarded cells SKIP 77). Aware-but-not-actionable: `bcast_string:135`/`bcast_strings:147` carry the same narrowing for string byte-lengths (≤120 chars per spec:164) — a different category the spec's element-count sentence does not cover; deliberately left uncommented, no follow-up item needed. No host quirks this iteration.

- [x] Resolve the `TableMeta<D>.kind`/`.OS` orphaned-metadata decision
  - spec: amrex-device-interface.md + CLAUDE.md no-placeholder discipline
  - tests: if wired — the new consumer's own test exercises the metadata path; if trimmed/documented — existing suite still green (removal is invisible).
  - notes: DONE (2026-07-07, build loop): decision = DOCUMENT, applied once as an extension of the existing design comment above `struct TableMeta` (`src/core/wli_table.H:48-59`). Rationale recorded in the comment itself: wire has no natural cheap consumer (`kind` is a runtime HDF5 value so it can't drive the kernels' static axis-kind choices — wiring means runtime dispatch no spec asks for; `amrex-device-interface.md:112` names per-axis metadata packaging an implementation freedom) and trim collapses `TableMeta` to just `n` (loses the only distinction from `TableView`, forces edits in `test_table_residency.cpp`/`test_parallelfor_wrappers.cpp` for zero behavioral gain). The comment states honestly: no `src/` code bridges a `HostEosTable` into a `TableMeta` today (only tests populate the slots); `kind`/`OS` are the convention slots such a builder WOULD fill; `view()` intentionally forwards only `n`+`data`; the `AxisKind` enum is live in the IO layer (`HostAxis::kind`/`HostOpacityGrid::kind`). Two builder-draft inaccuracies caught at orchestrator review and fixed before commit: the draft claimed TableMeta's fields are digest-covered (the rank digest folds the IO-layer kinds, not TableMeta's) and used indicative mood for the nonexistent IO→TableMeta bridge. Comment-only; no struct/kernel/test/spec change; default tree 32/32 green (7 guarded SKIPs). No host quirks.

- [x] Resolve `wli::PentaLinear` dead code
  - spec: CLAUDE.md no-dead-code / standard-library discipline (no spec mandates a full-5D pentalinear entry point)
  - tests: none if annotated; if removed, existing suite still green.
  - notes: DONE (2026-07-07, build loop): decision = ANNOTATE, applied once as a 3-line rationale comment appended to the existing PentaLinear header comment (`src/core/wli_interp.H:130-133`) — kept because it is the top rung of the declared basis ladder (`:13`) with a live oracle citation (`wlInterpolationUtilitiesModule.F90:487-517`); removal would break the design narrative for zero correctness gain. Comment states: kept for oracle-parity completeness, no current caller, every 5D channel fixes the non-thermodynamic axes and uses the 2D2D-aligned BiLinear (NES/Pair, Brem) or TetraLinear (EmAb/Iso) leaf. Function body/signature, ladder comment, and the "Penta partials out of scope" note (now `:158-159`) untouched. Research confirmed exhaustively: zero callers/tests/CI/thorn references; only spec mention is the Fortran-primitive inventory at `fortran-parity-and-tolerances.md:27` (documentation, not a mandate); no test asserts on the header's symbol set. Comment-only; default tree 32/32 green (7 guarded SKIPs). No host quirks (build cache was healthy, no reconfigure needed).

- [x] Rewrite `README.md` from its 2-line stub
  - spec: none — documentation hygiene, not spec-gated
  - tests: none.
  - notes: DONE (2026-07-07, build loop): full rewrite (53 lines) — verbatim mission paragraph + shape prose (`wli::`/`wli_` convention, `wli::Real` pinned to double, EOS trilinear evaluate/differentiate/inversion, EmAb+Iso / NES-Pair / Brem under the `wli_opacity.H` umbrella, one-shot device upload via `TableView`); `specs/README.md` named as acceptance source of truth; terse 3-command quickstart (configure/build/test, `tools/q`-prefixed) deferring to CLAUDE.md Build & run; variants/CI defer to `docs/BUILD.md`; one-line Cactus-thorn section linking `specs/cactus-integration.md`; LGPL-2.1 line (LICENSE confirmed present). CI badge included — owner/repo resolved from the live remote (`git remote get-url origin` → `lwJi/WeakLibInterp`, workflow `.github/workflows/ci.yml`, name: CI), not guessed. Deliberately excluded (drift/host-local): spec/test/target counts, the host `WL_TABLES_ROOT` path, the `WLI_*` knob table, `../amrex` as a hard requirement. Suite confirmed green after the change (32 tests, 0 failed, 7 guarded SKIPs). No host quirks.

## Decisions & non-blocking spec items

These are decisions/notes, NOT build increments — the responsible increment resolves each once and reuses it.

- **Coverage-matrix registration of the derivative entry points — DECIDED this plan run: not registered.** The prior plan deferred this decision here. The regression-suite researcher found no matrix inconsistency; the derivative kernels are covered by their dedicated `test_*_diff_point.cpp` targets plus the real-table FD cells. No spec-author dispatched on `regression-suite-design.md`. Revisit only if that spec gains an exhaustiveness assertion over entry points.
- **`specs/opacity-differentiate.md` stale-prose repair dispatched this plan run** (spec-author; descriptive "still-absent"/"dead" language vs implemented `dTetraLineardX1..4`/`dBiLineardX1/2` at `src/core/wli_interp.H:197-259`; no normative change). Build loop only needs to keep `validate_specs.sh` green.
- **Derivative kernels' "missing spec anchor" is a non-issue:** the synthesis flagged that `opacity-emab-iso.md` covers only the evaluate `_Point`s — but the differentiate contract's anchor IS `specs/opacity-differentiate.md`. No spec gap.
- **`/EC_table` group** in the EmAb fixture is still not among the 8 coverage rows — undecided 9th cell vs out of scope (`regression-suite-design.md` is silent; `HostEmAbTable::hasECTable` is read and digested but has no correctness cell). Carried from the prior plan.
- **Node-identity carve-out** (option (b) of the Tier 9 reconcile item) is a spec repair — if the build loop determines the machine tier cannot hold on real data, it records that in Discovered with the measured error and the plan loop dispatches the spec-author; it does not edit the four leaf specs itself.
- **Small polish, flag-only:** `wli::wli_value_type_size()` (`src/core/wli.cpp:12`) is an unused link-anchor deliberately odr-used by the CI installed-prefix scratch TU (`test_scaffold.cpp` re-derives `sizeof(wli::Real)==8` locally); no shared helper derives `EosInversionBounds` from a loaded table — spec leaves bounds representation free (`eos-inversion.md:163,169`), every caller hand-builds by scanning sub-table extents. Neither is mandated; touch only if an increment is already in the area (anti-duplication discipline).
- **`build.sh` `AMREX_ENABLE_MPI` variable name (Cactus thorn)** is unverifiable without a real ET `ExternalLibraries-AMReX` checkout (absent here; sibling `../amrex` has no such var). Non-blocking — a wrong guess hard-errors at the nested CMake consistency guard, no silent mislink.
- **Resolved by spec — do NOT build:** chunked-broadcast IMPLEMENTATION (`table-format-and-io.md:171`, all pinned tables far below INT_MAX — the Tier 10 item adds only the deferral comment); no external Fortran numeric fixture (self-contained scheme is by design, `fortran-parity-and-tolerances.md:135-142`); Cactus Verification #5 / official ET packaging / HDF5 C-API port / consumer thorn / ctest-inside-Cactus (`cactus-integration.md:15-19,91-98`); no combined MPI×GPU CI job; no parallel HDF5.

## Discovered since last plan

- [ ] Fix the same-class stale "no HDF5 reader / DEFERRED" comment in `test/test_emab_point.cpp`
  - spec: regression-suite-design.md (comment hygiene; no behavior change)
  - tests: none — comment-only; existing suite stays green.
  - notes: Found during the Tier 10 four-file comment fix (2026-07-07) but explicitly out of that item's scope. Same fix pattern: rewrite only the stale DEFERRED tail to forward-reference the real-table EmAb coverage in `test_production_tables.cpp` (via `read_emab_table` in `src/io/wli_io_opacity.{H,cpp}`), preserving the accurate "hand-rolled harness / synthetic table / no amrex::Initialize" leading clause; confirm the exact line range by grep before editing.
