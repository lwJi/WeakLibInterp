# TODO — WeakLibInterp build plan

Greenfield: no `src/`, no `test/`, no build system exists in the tracked tree yet. Goal is a GPU-friendly C++ reimplementation of weaklib's EOS and opacity interpolators exposed as AMReX-native device functions. All 10 acceptance specs are committed and internally consistent; the synthesis (`.research/synthesis.md`) found no missing spec. This plan decomposes the whole effort into dependency-ordered increments — foundations first (`src/lib` shared math + build scaffold), then EOS, then opacity kernels, then HDF5 readers, then the regression suite. Every increment is status **missing/new**; none is partial. List order within each group = build priority.

## Standing facts every item inherits

- **Value type pinned to `double`** on the entire correctness-bearing path, independent of `amrex::Real`. Entry points take `double const*` tables and `double` scalars; residency is `Gpu::DeviceVector<double>`. Never let `amrex::Real` leak into the math. (`build-integration.md:53`, `amrex-device-interface.md:90-96`)
- **Build target:** AMReX **CPU-only** (`AMReX_GPU_BACKEND=NONE`), **double precision** (`AMReX_PRECISION=DOUBLE`), **host execution, no Fortran/Matlab** at build or runtime. AMReX is a required dependency resolved from sibling `../amrex` (env override else `$PARENT/amrex`); probe header `amrex/Src/Base/AMReX_GpuContainers.H`. AMReX forces C++20; `AMReX_MPI` defaults ON and must be forced OFF via `CACHE … FORCE` before `add_subdirectory`. Library + regression suite both link AMReX + C++ HDF5. On RAM-constrained hosts cap parallel build (`-j4`). (`build-integration.md:39,45-48,52-55`)
- **Log-space + offset storage:** tables store `log10(value + offset)`; recovery is `physical = 10**(stored) - offset`. Axis kinds: **log** (ρ, T, E, η), **linear** (Yₑ, pre-`LOG10`'d opacity coords), **not-interpolated** (moment/kernel/species integer axes). NaN propagates naturally when a log argument ≤ 0; no range checks / aborts / guards anywhere on the interpolation path.
- **Bracket/delta:** `GetIndexAndDelta_{Log,Lin}` = `Index1D_*` bracket then `delta = (x - x[i])/(x[i+1]-x[i])`. **Clamp the INDEX** to `[lo, hi-1]` (`MAX(lo,MIN(hi-1,·))`); **do NOT clamp the delta** — that is exactly how edge extrapolation happens. Oracle: `weaklib/Distributions/Library/wlInterpolationUtilitiesModule.F90`, `wlInterpolationModule.F90`.
- **Tolerance tiers** (`fortran-parity-and-tolerances.md:40-88`): default parity `rtol=1e-12, atol=1e-30` (single interpolated value vs named `_Point` oracle); **relaxed `1e-10`** for analytic derivatives and inversion-recovered T; **machine-precision `~1e-14`** for closed-form self-contained invariants. Pass/fail: `|got-expected| <= rtol*|expected| + atol`; assert-and-fail (never print-only). The shared comparator lives in `src/lib` (built in the interpolation-core increment) and is reused by every leaf test.
- **Column-major flat indexing** (`amrex-device-interface.md:64-78`): h5ls prints C-order → reverse to Fortran order. One formula `i0 + n0*(i1 + n1*(i2 + n2*(...)))` for 1D–5D, used identically by kernels and reader. EOS 3D `t[iD + nD*(iT + nT*iY)]`; EmAb 4D `(nE,nρ,nT,nYe)`; opacity 5D e.g. `iEp + nE'*(iE + nE*(k + nMom*(iT + nT*iEta)))`.
- **Device conventions** (`amrex-device-interface.md:36-88`): every `_Point` is `AMREX_GPU_HOST_DEVICE AMREX_FORCE_INLINE`, scalar, allocation-free, table passed as `double const*` + int extents. `TableData`/`Table1D–4D` **cap at 4D** (no `Table5D`), so 5D opacity channels must use flat `double const*`+extents — use the flat-pointer convention **uniformly, even for 3D EOS**. Residency: host-filled `Gpu::DeviceVector<double>` uploaded once via `Gpu::htod_memcpy`; per-axis metadata via `GpuArray`. Array form = `ParallelFor` wrapper over the `_Point` core (host launch).
- **HDF5 layout** (`table-format-and-io.md`; 6 committed `specs/fixtures/*.h5ls` match every name/shape exactly): read `/DependentVariables/Names` before opening named value arrays; index by name/slot, not fixed integer. EOS `/DependentVariables/Offsets` 1D `[15]`; EmAb `/EmAb/Offsets` 1D `[nOpacities]`; scattering kernels (Iso/NES/Pair/Brem) `Offsets` 2D `[nOpacities,nMoments]`. Shared `/EnergyGrid` (`Values{40}`), `/EtaGrid` (`Values{120}`, NES/Pair only). Fixture shapes (Fortran order): EOS DVs `(30,81,185)=(Yₑ,T,ρ)`; EmAb `(40,185,81,30)`; Iso `(40,2,185,81,30)`; NES/Pair Kernels `(40,40,4,81,120)`; Brem `S_sigma (40,40,1,185,81)`.

## Tier 0 — Foundations (`src/lib` + build scaffold; blocks everything)

- [x] Build scaffold + value-type pin — stand up the CPU-only AMReX+HDF5 build with `src/lib/` and `test/` layout
  - spec: build-integration.md — acceptance source of truth
  - tests: (#1) CPU-only build of library + suite targets succeeds against `../amrex`; (#2) no Fortran/Matlab in toolchain (C++/HDF5-C++ only); (#4) AMReX source resolves from the sibling checkout; (#5) `AMREX_ROOT=../amrex bash specs/tools/validate_specs.sh` passes; `static_assert` that the pinned value type is `double` (not `amrex::Real`).
  - notes: DONE — all five checks pass. Landed: top-level `CMakeLists.txt` (C++20, `project(... CXX)`, force-block `AMReX_GPU_BACKEND=NONE`/`AMReX_PRECISION=DOUBLE`/`AMReX_MPI=OFF`/`AMReX_FORTRAN=OFF`/`AMReX_OMP=OFF`/`AMReX_INSTALL=OFF` + solvers/amrlevel/particles/tiny-profile OFF, all `CACHE … FORCE` before `add_subdirectory(../amrex)`); `src/lib/wli_real.H` pins `wli::Real = double` with `static_assert` (never references `amrex::Real`); `wli_lib` static lib PUBLIC-links `AMReX::amrex`; `wli_tests` links `hdf5::hdf5_cpp` (fallback `${HDF5_CXX_LIBRARIES}`); ctest `scaffold` test. Build dir is `build/` (gitignored); `.build/` stays agent scratch. Do NOT enable `AMReX_HDF5` — find HDF5 independently to avoid AMReX's parallel-HDF5 enforcement. Host toolchain was installed via apt (`cmake`, `g++`, `libhdf5-dev` with C++ bindings at `/usr/include/hdf5/serial/H5Cpp.h`); reinstall if the sandbox is reset. Commands recorded in `CLAUDE.md` Build & run.

- [x] Column-major flat-index helper (1D–5D) in `src/lib`
  - spec: amrex-device-interface.md — acceptance source of truth (§:64-78)
  - tests: index round-trip — write sentinels via the flat-index formula into a `DeviceVector<double>`, read back through the helper for 3D/4D/5D shapes; exact equality.
  - notes: DONE — landed `src/lib/wli_index.H`: `wli::flat_index<int D>(GpuArray<int,D> idx, GpuArray<int,D> n)`, `AMREX_GPU_HOST_DEVICE AMREX_FORCE_INLINE`, returns `amrex::Long`, Horner fold uniform over D∈{1..5} (`static_assert` bounds D). Include `<AMReX_Array.H>` (pulls both qualifier macros + `GpuArray`; `AMREX_FORCE_INLINE` alone lives in `<AMReX_Extension.H>`, not `<AMReX_GpuQualifiers.H>`). Test `test/test_index_roundtrip.cpp` (ctest `index_roundtrip`) covers 1D–5D round-trip, corners, bijection, and `ParallelFor` kernel-callability; on this CPU-only build `Initialize/Finalize` + `ParallelFor` collapse to sequential/no-op. Header-only ⇒ no `src/lib/CMakeLists.txt` change (PUBLIC include dir suffices). Downstream `_Point` kernels and readers must reuse `wli::flat_index` — extents carrier stays `GpuArray<int,D>`, agnostic to the residency increment's metadata convention.

- [x] Interpolation math core (`src/lib`, header-only, `AMREX_GPU_HOST_DEVICE`) + shared comparator
  - spec: fortran-parity-and-tolerances.md — acceptance source of truth (§:66-88)
  - tests: node identity (query at a node ⇒ index/delta reproduce the stored node exactly); affine-in-log exactness `~1e-14` (interpolate a synthetic affine-in-log table, recover input); clamp-index boundary behavior (out-of-range ⇒ index clamped, delta unclamped/extrapolating); NaN propagation on non-positive log argument.
  - notes: DONE — ctest `interp_core` covers all four required behaviors + a comparator self-check; full suite 3/3 green. Landed `src/lib/wli_interp.H`: `ln10`; `GetIndexAndDelta{Lin,Log}` returning `IndexDelta{i,d}` (0-based port of `Index1D_*`+`GetIndexAndDelta_*`, `std::floor` before int cast, index clamped to `[0,n-2]`, delta never clamped; pre-`LOG10`'d coords just use the Lin variant); **named recursively-factored basis** `Linear→BiLinear→TriLinear→TetraLinear→PentaLinear` (design decision resolved: named functions, not templated N-D; corners as unpacked scalars, caller gathers via `wli::flat_index`); partials `dBiLineardX1/2`, `dTriLineardX1/2/3` (Tetra/Penta partials deliberately absent — no oracle counterpart); `recover(f,OS)=10**f-OS`. All `AMREX_GPU_HOST_DEVICE AMREX_FORCE_INLINE noexcept`, allocation-free. **Corner bit-order convention (binding on all leaf gathers):** label `b1b2b3…` → argument slot `b1+2*b2+4*b3+…`, dX1 fastest/least-significant, matching Fortran arg order + column-major. `src/lib/wli_compare.H`: tiers `rtol_parity=1e-12`, `atol_default=1e-30`, `rtol_relaxed=1e-10`, `rtol_machine=1e-14`; `is_close(got,expected,rtol,atol)`. Faithfulness detail: at a node the bracket may land in the cell below, giving delta exactly 1 instead of 0 — both bit-exact. Chain-rule scale factors + `(value+OS)` reconstitution remain with the EOS evaluate-and-differentiate item (as already scoped).

- [x] Device table-residency / upload helper + qualifier convention
  - spec: amrex-device-interface.md — acceptance source of truth (§:36-44,80-88)
  - tests: device/host equivalence — `_Point` called directly on host equals the same call inside `ParallelFor` (machine-precision/exact); upload round-trip (host buffer → `htod_memcpy` → read back).
  - notes: DONE — ctest `table_residency` covers 3D/4D/5D upload round-trip (exact `==`), host-vs-`ParallelFor` equivalence (capture-by-value, `rtol_machine`), and trivially-copyable `static_assert`s; suite 4/4 green. Landed `src/lib/wli_table.H` (header-only; no `src/lib/CMakeLists.txt` change): **metadata convention pinned** — `wli::AxisKind {NotInterp=0,Linear=1,Log=2}`; `TableMeta<D>{ GpuArray<int,D> n; GpuArray<AxisKind,D> kind; double OS; amrex::Long size() }`; `TableView<D>{ double const* data; GpuArray<int,D> n; operator()(idx) via wli::flat_index }` — the trivially-copyable by-value handle every leaf kernel captures (never `Table*D`/`Array4`); `ResidentTable<D>` owns `Gpu::DeviceVector<double>` + `upload(meta,host,count)` via `Gpu::htod_memcpy` (byte count = `count*sizeof(double)`), exposes `view()`/`meta()`. Multi-offset opacity channels carry a separate device-resident offsets array selected by the component axis — `TableMeta` holds only the single scalar `OS`. CPU-only build: `DeviceVector` = `PODVector<double,std::allocator>` — no arena accessors, no zero-init (fill host buffers fully before compare); `GpuArray<AxisKind,D>` aggregates fine and stays trivially copyable. Downstream leaf kernels (3D EOS, 4D EmAb, 5D Iso/NES/Pair/Brem) take `TableView<D>` + `TableMeta<D>`.

## Tier 1 — EOS interpolation & inversion

- [x] EOS single-point evaluate `_Point` (trilinear-in-log)
  - spec: eos-interpolation.md — acceptance source of truth
  - tests: affine-in-log exactness `~1e-14`; node identity; boundary extrapolation (clamp index not delta); NaN propagation; production-table parity vs oracle `rtol=1e-12,atol=1e-30` (deferred to the regression-suite production cells).
  - notes: DONE — ctest `eos_point` green (affine-in-log interior ~1e-14; node identity interior + top-edge iD=nD-1; boundary extrapolation below/above both ρ and T edges exact under affine-in-log; NaN on ρ≤0 and T≤0); full suite 5/5. Landed `src/lib/wli_eos.H`: `wli::EosInterpolateSingleVariable3DPoint(D,T,Y, Ds,nD, Ts,nT, Ys,nY, OS, table)` — inputs are RAW physical values (kernel takes log10 internally on ρ,T; Yₑ raw), composes `GetIndexAndDeltaLog/Lin` + 8-corner gather via `flat_index<3>` (ρ = axis 0 = dX1 fastest) + `TriLinear` + `recover`; `AMREX_GPU_HOST_DEVICE AMREX_FORCE_INLINE noexcept`, allocation-free, no guards. Header-only, flat in `src/lib`, no CMake change beyond `test/CMakeLists.txt` registration (`test/test_eos_point.cpp`, synthetic table nD=4,nT=3,nY=3, OS=3.5, no HDF5). Oracle `LogInterpolateSingleVariable_3D_Custom_Point` (`wlInterpolationModule.F90:1640-1707`) → `LinearInterp3D_3DArray_Point` (`wlInterpolationUtilitiesModule.F90:630-660`). The differentiate increment should reuse this corner gather + `dTriLineardX*`; production `.h5` parity stays with the regression-suite umbrella (as scoped).

- [x] EOS evaluate-and-differentiate `_Point`
  - spec: eos-interpolation.md — acceptance source of truth
  - tests: derivative chain-rule vs finite-difference, **relaxed `1e-10`** (`∂/∂ρ,∂/∂T,∂/∂Yₑ`); value component identical to the evaluate `_Point`; boundary/NaN as evaluate.
  - notes: DONE — ctest `eos_diff_point` green (analytic affine closed forms at 2 interior cells; 4th-order FD cross-check on a NON-affine table, all 3 partials, rtol 1e-10; value exact-`==` to evaluate at interior/node/out-of-edge; boundary derivative extrapolation both ρ/T edges; NaN into value + all 3 derivatives on ρ≤0/T≤0, finite on Yₑ≤0); full suite 6/6. Landed in `src/lib/wli_eos.H`: `EosPointDeriv{value,dDrho,dDT,dDY}` (order = Fortran `Derivative(1:3)`) + `EosInterpolateDifferentiateSingleVariable3DPoint(...)` sharing evaluate's bracket/gather/TriLinear/recover (value bit-identical), partials `recon·a_k·dTriLineardX_k` with `a_D=1/(D·log10(Ds[iD+1]/Ds[iD]))`, `a_T=1/(T·log10(Ts[iT+1]/Ts[iT]))`, `a_Y=ln10/(Ys[iY+1]-Ys[iY])` (ln10 on the LINEAR axis only), `recon=value+OS` (oracle op order). Oracle `LogInterpolateDifferentiateSingleVariable_3D_Custom_Point` (`wlInterpolationModule.F90:1814-1844`) → `LinearInterpDeriv3D_3DArray_Point` (`wlInterpolationUtilitiesModule.F90:912-964`). Note for the FD pattern: plain 2-point central diff only reaches ~1e-9 — use the 4th-order stencil with `h≈1e-3·x` kept inside one cell. Spec's FD-on-real-table half deferred to the regression-suite umbrella (no `.h5` loader yet), mirroring evaluate's deferred production parity.

- [x] EOS inversion core — DEY family (NoGuess + Guess)
  - spec: eos-inversion.md — acceptance source of truth
  - tests: round-trip invariant on recovered T and re-evaluated X, **relaxed `1e-10`** (`:113-128`); affine-in-log inverse `~1e-14`; error-code exact-equality for all codes `{0,01,02,03,10,11,13}` in the mandated cascade order; `T=0` sole failure signal (`_NoError`); highest-T-root (NoGuess) vs nearest-to-guess (Guess) root selection on non-monotone/no-bracket.
  - notes: DONE — ctest `eos_inversion_dey` green (7 checks: affine-in-log inverse ~1e-14 via NoGuess, Guess Step-1 hit, Guess Step-2 full-range bisection; round-trip 1e-10 both wrappers; error codes 1/2/3/10/11 exact + T==0 both wrappers; code 13 no-root; highest-T vs nearest-to-guess root selection on a non-monotone table); full suite 7/7. Landed `src/lib/wli_eos_inversion.H` (header-only): `EosInversionBounds{MinD,MaxD,MinX,MaxX,MinY,MaxY,initialized=false}` (no T bounds — cascade never reads them); `EosInversionResult{T,Error}`; `CheckInputError` cascade uninitialized(10)→NaN-self-inequality D,X,Y(11)→D(1)→X(2)→Y(3)→0; `InverseLogInterp(T_a,T_b,X_a,X_b,X,OS)` closed form; `EosInvertFaceEval` fixed-T-node (ρ,Yₑ) bilinear-in-log face on the 3D layout; **generic `ComputeTemperatureWith_DXY_{NoGuess,Guess}` kernels** (integer node bisection, convergence `i_b==i_a+1`, no float tolerance; `MAX(i_a+1, (i_a+i_b)/2)` midpoint guard; Guess = guess-cell → full-range bisection with guess-biased first midpoint → nearest-to-guess ascending scan whose running window freezes on record — quirks are load-bearing on non-monotone tables); thin DEY wrappers (check-first, T pre-zeroed and re-zeroed on any Error≠0). Two distinct offset sites kept separate: `-OS` via `recover` in the face eval vs `+OS` inside `InverseLogInterp`. `(iD,dD)/(iY,dY)` computed ONCE per call (full-axis delta ≡ Fortran 2-element sub-bracket delta) and reused across all T-node faces. Oracle verified line-for-line: `wlEOSInversionModule.F90` NoGuess :469-571, Guess :299-438, CheckInputError :198-227, InverseLogInterp :253-267; face `wlInterpolationModule.F90:1129-1165`. Test `test/test_eos_inversion_dey.cpp` (no amrex::Initialize, no HDF5). Next inversion increment is wrapper-only over these `_DXY` kernels.

- [x] EOS inversion family generalization (DPY, DSY) + `_Error`/`_NoError` reporting wrappers
  - spec: eos-inversion.md — acceptance source of truth
  - tests: DPY and DSY families reproduce the same round-trip/error-code/`T=0` checks as the DEY core; `_Error` returns the code, `_NoError` signals only via `T=0`.
  - notes: DONE — ctest `eos_inversion_family` green (16 checks: the 7 DEY-core behaviors — affine-in-log inverse ~1e-14 NoGuess/Guess-Step-1/Guess-Step-2, round-trip 1e-10, error codes 1/2/3/10/11 + 13 no-root exact with `T==0`, highest-T vs nearest-to-guess root selection — run for BOTH DPY and DSY via a templated `run_family()`, plus a `_NoError` contract check per family); full suite 8/8, no regressions. Landed in `src/lib/wli_eos_inversion.H`: `ComputeTemperatureWith_{DPY,DSY}_{NoGuess,Guess}` struct-returning wrappers (bodies byte-identical to DEY apart from the substituted dependent scalar + sub-table pointer; oracle DPY `wlEOSInversionModule.F90:760-861`, DSY `:904-1005`) + six `ComputeTemperatureWith_{DEY,DPY,DSY}_{NoGuess,Guess}_NoError` bare-`Real` forwarders returning only `.T`. **Design decision (spec `:162` freedom):** the struct-returning `EosInversionResult{T,Error}` form IS the `_Error` reporting form — unsuffixed names kept (no `_Error` aliases, no rename of the DEY entry points); `_NoError` = one-liner forwarder, `T==0` sole failure signal. One `EosInversionBounds` serves E/P/S (`MinX/MaxX` generic). Generic `_DXY` kernels untouched. Still open elsewhere: `_Many`/`ParallelFor` array wrappers (residency layer) and optional host-side `DescribeEOSInversionError` string mapping. Test `test/test_eos_inversion_family.cpp` (synthetic tables, no HDF5, no `amrex::Initialize`).

## Tier 2 — Opacity kernels (mutually independent after the math core)

- [x] EmAb 4D `_Point` (tetralinear-in-log)
  - spec: opacity-emab-iso.md — acceptance source of truth
  - tests: affine-in-log exactness `~1e-14`; node identity; boundary extrapolation; NaN propagation; production parity `rtol=1e-12,atol=1e-30` (via regression suite).
  - notes: DONE — ctest `emab_point` green (14 checks: affine-in-log interior ~1e-14; node identity interior + top-edge all-axes-at-n-1; boundary extrapolation below/above ALL four axes incl. Yₑ; NaN on non-positive E/ρ/T; explicit no-NaN on Yₑ≤0 — linear axis extrapolates); full suite 9/9, no regressions. Landed `src/lib/wli_opacity.H`: `wli::EmAbInterpolateSingleVariable4DPoint(LogE,LogD,LogT,Y, LogEs,nE, LogDs,nD, LogTs,nT, Ys,nY, OS, table)` — **inputs pre-`LOG10`'d by the caller** (E,ρ,T; Yₑ raw), so `GetIndexAndDeltaLin` on ALL FOUR axes (never `...Log`; kernel calls no `log10` — key divergence from the 3D EOS kernel, which logs internally); 16-corner gather via `flat_index<4>` E fastest (bit1=E least-significant per the corner convention) + `TetraLinear(dE,dD,dT,dY)` + `recover(f,OS)`; `AMREX_GPU_HOST_DEVICE AMREX_FORCE_INLINE noexcept`, allocation-free, no guards. `OS` is a plain scalar the caller pre-selects from 1D `Offsets[nOpacities]` — no offset array/species index in the leaf. Oracle `LogInterpolateSingleVariable_4D_Custom_Point` (`wlInterpolationModule.F90:1754-1779`) → `LinearInterp4D_4DArray_Point` (`wlInterpolationUtilitiesModule.F90:729-768`). `src/lib/CMakeLists.txt` needs no header enumeration (include-dir based). Test-fixture lesson: for tetralinear-in-log exactness the STORED value must be affine in the log coords (store `affine(...)` directly, expect `recover(affine,OS)`; storing `log10(affine+OS)` breaks off-node exactness), and keep log-space values modest (~0.6..1.6) so `10**f` doesn't amplify ULP error past 1e-14 under extrapolation. The Iso increment slices its 5D table to 4D and calls THIS kernel with `Offsets(species,moment)`. Production `.h5` parity stays with the regression-suite umbrella.

- [x] Iso 5D-slice-to-4D `_Point`
  - spec: opacity-emab-iso.md — acceptance source of truth
  - tests: moment-slice independence (each fixed `(species,moment)` slice interpolates independently); reuse of the EmAb kernel gives identical affine/node/boundary/NaN behavior.
  - notes: DONE — ctest `iso_point` green (22 checks: affine-in-log ~1e-14 at iMom=0 AND iMom=1; node identity interior + top-edge; moment-slice independence incl. slices-differ and offset-not-transposed guards; boundary extrapolation all 4 axes; NaN on non-positive E/ρ/T, none on Yₑ≤0); full suite 10/10. **Key discovery:** a fixed-`iMom` slice of the 5D layout `(nE,nMom,nD,nT,nY)` is STRIDED (moment is axis 1 ⇒ D-stride `nE*nMom`), so the shipped EmAb 4D kernel canNOT be called as a pointer-offset black box when `nMom>1` — its single `nE` param serves both bracket length and stride. Resolved per spec `:154` freedom: landed sibling `wli::IsoInterpolateSingleVariable5DPoint(LogE,LogD,LogT,Y, LogEs,nE,…,Ys,nY, iMom,nMom, OS, table)` in `src/lib/wli_opacity.H` — same primitives (`GetIndexAndDeltaLin`×4 pre-LOG10'd, 16-corner gather, `TetraLinear`, `recover`), same E=dX1(LSB)/ρ/T/Yₑ corner-bit order, only difference is `flat_index<5>` with `iMom` held fixed in every corner. EmAb kernel untouched. Plus `wli::IsoOffset(offsets,nOpacities,nMoments,iSpecies,iMom)` = column-major `iSpecies + nOpacities*iMom` (species-major/moment-minor; do NOT transpose). Out of the leaf contract (consumer concerns for later loops): `QueryOpacity_Iso` density-threshold guard, `UnitES` post-scaling, `_Many`/`ParallelFor` array form, production `.h5` parity (regression-suite umbrella). Test `test/test_iso_point.cpp` synthetic-only (nMom=2 mandatory to exercise the stride), no HDF5.

- [x] NES/Pair aligned 2D2D bilinear `_Point` (one shared primitive)
  - spec: opacity-nes-pair.md — acceptance source of truth
  - tests: affine-in-log exactness `~1e-14`; node identity; kernel-slice independence; boundary extrapolation; NaN propagation; production parity `rtol=1e-12`.
  - notes: DONE — ctest `nes_pair_point` green (16 checks: affine-in-log interior ~1e-14; node identity interior + top-edge; kernel-slice independence kernel=0/1/2 incl. slices-differ guard; energy-index independence + distinct-(iE',iE) no-cross-talk + explicit `iE'>iE` exact recovery, proving NO triangle guard in the leaf; boundary extrapolation below/above both T and η edges; NaN on non-positive T/η); full suite 11/11, no regressions. Landed in `src/lib/wli_opacity.H`: `wli::NESPairInterpolateSingleVariable2D2DAlignedPoint(LogT,LogX, LogTs,nT, LogXs,nEta, iEp,iE,nEp,nE, kernel,nMom, OS, table)` — ONE channel-neutral kernel serving both NES and Pair; compose-only leaf: `GetIndexAndDeltaLin`×2 (T,η pre-`LOG10`'d by caller — kernel calls no `log10`) → 4-corner gather via `flat_index<5>` on `(nE',nE,nMom,nT,nη)` with `(iE',iE,kernel)` FIXED and only iT/iη toggling → `BiLinear(p00,p10,p01,p11,dT,dη)` (dX1=T fastest per corner convention) → `recover(f,OS)`; no guards of any kind (`iE'>iE` valid — the Fortran `i≤j` triangle loop is consumer-side, deferred to the symmetry fills). Offset selection stays caller-side via existing channel-agnostic `wli::IsoOffset` (2D `Offsets[nOpacities,nMoments]`; pinned tables nOpacities=1). **Oracle correction (important):** the spec-named `LogInterpolateSingleVariable_2D2D_Custom_Aligned_Point` (`wlInterpolationModule.F90:1455-1485`) is the triangle-looping wrapper — the true 1:1 scalar core is `LinearInterp2D_4DArray_2DAligned_Point` (`wlInterpolationUtilitiesModule.F90:602-627`); port the latter. Build gotcha: adding a new test target needs a `cmake -S . -B build` reconfigure before `cmake --build`. Test `test/test_nes_pair_point.cpp` synthetic-only (distinct affine per `(iE',iE,kernel)` triple), no HDF5. Production `.h5` parity stays with the regression-suite umbrella. The NES/Pair symmetry-fill increments consume THIS primitive's lower-triangle output.

- [x] NES detailed-balance symmetry fill
  - spec: opacity-nes-pair.md — acceptance source of truth (§:150-156)
  - tests: closure check — upper triangle satisfies `Phi(iEp,iE) = Phi(iE,iEp)·exp((E(iE)-E(iEp))/T)` for `iEp>iE`.
  - notes: DONE — ctest `nes_detailed_balance` green (closure vs `lower*exp((E[iE]-E[iEp])/T)` at 5 `iEp>iE` pairs, Boltzmann factor <1, sign/index-order guard vs the reciprocal, diagonal `iEp==iE` fixed point = primitive value); full suite 12/12, no regressions. Placement freedom (`:216`) resolved: shared `src/lib` leaf `wli::NESDetailedBalanceFillPoint(...)` in `src/lib/wli_opacity.H:249-291` — compose-only: calls `NESPairInterpolateSingleVariable2D2DAlignedPoint` at SWAPPED energy indices `(iE,iEp)` (reads the lower triangle) then multiplies by `std::exp((E[iE]-E[iEp])/T)`; NO triangle guard in the leaf (diagonal is the factor=1 fixed point; caller applies for `iEp>=iE`). Extra plumbing vs the primitive: physical (un-logged, MeV) `E[]` and `T` — real-table `/ThermoState/Temperature` is Kelvin, so a future consumer must convert `kT = T*8.61733e-11 MeV/K` (out of leaf scope). Oracle `wlOpacityInterpolationModule.f90:104-115`. Test `test/test_nes_detailed_balance.cpp` synthetic-only (E={2,8,20,45} MeV, T=5 MeV), no HDF5. The Pair crossing-symmetry fill is deliberately untouched — separate item below.

- [x] Pair crossing-symmetry fill
  - spec: opacity-nes-pair.md — acceptance source of truth (§:160-172)
  - tests: closure check — upper triangle transposes energy indices and swaps `Ji↔Jii` kernel components (lower triangle unswapped).
  - notes: DONE — ctest `pair_crossing_symmetry` green (upper Phi0/Phi1 via leaf == direct primitive at swapped energies+swapped kernels over 5 `iEp>iE` pairs; distinctness guard vs un-swapped lower-triangle assembly; diagonal guard `iEp==iE` swapped≠unswapped); full suite 13/13, no regressions. Landed `wli::PairCrossingSymmetryFillPoint(...)` in `src/lib/wli_opacity.H:294-333` — compose-only: calls `NESPairInterpolateSingleVariable2D2DAlignedPoint` with energy indices SWAPPED `(iEp,iE)→(iE,iEp)` and the caller-supplied ALREADY-SWAPPED kernel component + its matching OS; NO Boltzmann factor, no scaling (spec:174 "exact relabeling"), no `C_i/C_ii` weighting (consumer assembly, spec:32,:221). **Key difference from the NES fill:** the diagonal is NOT a fixed point (the `Ji↔Jii` swap is not a no-op at `iEp==iE`) — caller applies this fill ONLY for `iEp>iE`; the diagonal stays on the unswapped lower-triangle path (oracle lower loop is inclusive of diagonal, f90:160-192). Kernel-component indexing (0-based): `{iJi0,iJii0,iJi1,iJii1}={0,1,2,3}`, swap = `k^1` within each moment order — no 0↔1 moment crossing; no named constants introduced (leaf stays bare-`int` like the NES sibling). Oracle upper triangle `wlOpacityInterpolationModule.f90:196-228`. Test `test/test_pair_crossing_symmetry.cpp` synthetic-only (nMom=4 mandatory — NES's nMom=3 can't populate the iJ*1 pair), no HDF5.

- [x] Brem 5D aligned summed `_Point`
  - spec: opacity-brem.md — acceptance source of truth
  - tests: affine-in-log exactness `~1e-14`; node identity; moment-slice independence; **effective-density decomposition closure** (sum of 3 weighted interps); weight-order sensitivity; **both energy triangles computed independently** (no symmetry fill / no transpose derivation); boundary extrapolation; NaN propagation; production parity `rtol=1e-12`.
  - notes: DONE — ctest `brem_point` green (8 checks: affine-in-log interior ~1e-14; node identity interior + top-edge; moment-slice independence; decomposition closure summed == `1·K1+1·K2+(28/3)·K3` with three distinct terms; weight-order sensitivity — permuted `[28/3,1,1]` differs; both triangles `(0,2)`/`(2,0)` each equal their OWN decomposition and differ, proving no fill/transpose; boundary extrapolation below ρ / above T edges; NaN on non-positive effective ρ and T); full suite 14/14, no regressions. Landed in `src/lib/wli_opacity.H`: inner `wli::BremInterpolateSingleDensity2DAlignedPoint(LogD,LogT, LogDs,nD, LogTs,nT, iEp,iE,nEp,nE, moment,nMom, OS, table)` — compose-only (ρ,T) bilinear at fixed `(iE',iE,moment)`: `GetIndexAndDeltaLin`×2 (pre-`LOG10`'d, no `log10` in leaf) → 4-corner gather via `flat_index<5>` on extents `{nEp,nE,nMom,nD,nT}` (**ρ before T — opposite of NES/Pair's (T,η); the main copy-paste hazard**) → `BiLinear(…, dD, dT)` (dX1=ρ fastest) → `recover`. Summed wrapper `wli::BremInterpolateSingleVariable2D2DAlignedSummedPoint(LogD[],Alpha[],nSpecies, …)`: `sum += Alpha[l]·inner(LogD[l],…)` — recovery `-OS` happens INSIDE the inner call per effective density BEFORE weighting (never weight in log space; oracle `wlInterpolationModule.F90:1559-1564`). Leaf stays channel-neutral: caller supplies the three effective `LogD` (`log10(D·Xp)`, `log10(D·Xn)`, `log10(D·√|Xp·Xn|)`) and `Alpha_Brem=[1,1,28/3]` — no hardcoded weights/loop count, no mass fractions in leaf, no triangle guard (every `(iE',iE)` independent). Units ρ g/cm³, T Kelvin (EOS convention, NOT NES/Pair's MeV-scale coords). Offset via channel-agnostic `wli::IsoOffset` (2D `Offsets[1,1]` in the pinned table). Oracle `SumLogInterpolateSingleVariable_2D2D_Custom_Aligned` (`wlInterpolationModule.F90:1488-1595`) → scalar core `LinearInterp2D_4DArray_2DAligned_Point` (`wlInterpolationUtilitiesModule.F90:602-627`). Test `test/test_brem_point.cpp` synthetic-only (3×3×3×5×4, asymmetric per-`(iE',iE,moment)` affine), no HDF5. Production `.h5` parity stays with the regression-suite umbrella. Tier 2 opacity kernels now complete.

## Tier 3 — HDF5 readers (parallelizable after the build scaffold; feed production-table cells)

- [ ] HDF5 reader: EOS table + shared in-memory table struct
  - spec: table-format-and-io.md — acceptance source of truth
  - tests: structural-conformance self-test — discovered group/dataset names+shapes match `specs/fixtures/wl-EOS-SFHo-15-25-50.h5ls` exactly (runs without the multi-GB table present).
  - notes: Read `/ThermoState` (Density{185},Temperature{81},Electron Fraction{30},LogInterp{3}), `/DependentVariables` (**Names before arrays**, 1D `Offsets[15]`, per-var min/max), `/Metadata`; produce host axis arrays + flat log-stored `double[]` per named DV ready for device upload, keyed by name/slot. Oracle `wlEOSIOModuleHDF.f90:183-213,489-496`, `wlIOModuleHDF.F90:882-1036`. **Decide the shared in-memory table struct (SoA vs flat+extents) once here** (freedom, `:187`) so all channel readers reuse it. Independent of the math/kernel increments; only gates production-table regression cells.

- [ ] HDF5 reader: EnergyGrid/EtaGrid + opacity channels
  - spec: table-format-and-io.md — acceptance source of truth
  - tests: structural conformance against `wl-Op-*-{EmAb,Iso,NES,Pair,Brem}.h5ls` (names, shapes, 1D-vs-2D `Offsets` dimensionality) exactly.
  - notes: Shared `/EnergyGrid` (optional geometric `Zoom/Edge/Width`, keyed off `Zoom` presence) + `/EtaGrid`; per-channel `/EmAb` (1D Offsets, `/EmAb_CorrectedAbsorption` legacy fallback, optional `/EmAb Parameters`,`/EC_table`), `/Scat_Iso_Kernels`, `/Scat_NES_Kernels`, `/Scat_Pair_Kernels`, `/Scat_Brem_Kernels` (2D Offsets). Reuses the EOS-reader in-memory struct. EmAb legacy fallback is implementation-freedom and moot for all pinned fixtures (all have `/EmAb`).

## Tier 4 — Regression suite & precision verification

- [ ] Regression-suite umbrella — coverage matrix + production loader/guard + meta-test
  - spec: regression-suite-design.md — acceptance source of truth
  - tests: coverage-matrix closure — **8 entry points × 4 regimes** (in-bounds / on-edge / out-of-range / NaN-input), each cell at its assigned tier; production-table cells run when the 6 named `.h5` tables are present and **skip distinctly (not fail)** when absent; a **deliberate-perturbation meta-test** proving an injected wrong value fails an assertion; no Fortran/Matlab at test time; `validate_specs.sh` closure gate passes.
  - notes: 8 rows = EOS evaluate, EOS evaluate+differentiate, EOS inversion, EmAb, Iso, NES, Pair, Brem (`regression-suite-design.md:40-51`). Builds synthetic in-memory generators (affine-in-log, constant, symmetry triangles — always-on primary fixtures) and a production-table loader/guard keyed off `specs/fixtures/tables.provenance` (path+sha256; snapshot_sha256 for CI, table_sha256 for refresh). Reuses the `src/lib` comparator. Downstream: assumes all 8 entry points + both readers exist. Framework/layout free (GoogleTest/Catch2/hand-rolled + CTest/custom).

- [ ] Value-type pin verification under single-precision AMReX (follow-on)
  - spec: build-integration.md — acceptance source of truth (§:63)
  - tests: machine-precision self-contained checks still pass with `amrex::Real=float`, proving the `double` pin holds (`~1e-14`).
  - notes: Explicitly deferred by all prior work. May need a second AMReX build config (`AMReX_PRECISION=SINGLE`) or may reduce to a `static_assert`-level structural guarantee — decide scope when reached. Small; trails the suite (needs the machine-precision checks to exist to re-run under single precision).

## Decisions & non-blocking spec items

These are decisions/notes, NOT build increments — the responsible increment resolves each once and reuses it.

- **Spec coverage is complete** — the synthesis confirmed all 10 committed specs cover the surface with verified Fortran-oracle citations; **no new spec is warranted**. Do not author specs on a "missing" basis.
- **In-memory table struct** (SoA vs flat+extents) — freedom (`table-format-and-io.md:187`); decide once in the EOS-reader increment, reuse everywhere.
- **N-D multilinear kernel** — single templated N-D vs named Tri/Tetra/Penta functions; freedom (`fortran-parity-and-tolerances.md:148-152`, `amrex-device-interface.md:107-113`). Decide once in the interpolation-math-core increment.
- **Per-axis metadata carrier** (`GpuArray` vs POD vs separate args) and the `ParallelFor` overload — freedom; pin one convention in the device-residency increment.
- **Inversion dispatch** — single templated core vs separate `DEY/DPY/DSY` entry points; freedom (`eos-inversion.md:162`).
- **EmAb legacy fallback mechanism** — spec allows link-exists probe vs try-open-catch on `/EmAb` else `/EmAb_CorrectedAbsorption`; Fortran actually branches on a parsed `nuclei_EC_table==-1` sentinel (`wlOpacityTableIOModuleHDF.f90:1157`). Moot for all pinned fixtures (all have `/EmAb`). Non-blocking; note for the opacity-channels reader.
- **`/EC_table` group** in the EmAb fixture is not among the 8 coverage rows — undecided whether it is a 9th cell or out of scope (`regression-suite-design.md` is silent; evidence: `wl-Op-…-EmAb.h5ls` carries `/EC_table`, no spec entry-point references it). Decide whether the regression-suite umbrella needs a 9th row when that increment is reached.
- **Build system + target names** — CMake and names like `weaklibinterp`/`weaklibinterp_suite` come only from a stale/orphaned `.build/` cache (a hint, not a spec fact; specs are silent on names). Its design (env-override-then-sibling AMReX probe, `CACHE…FORCE` pinning, C++20, `AMReX_MPI=OFF`, `-j4` OOM cap) is reusable as a starting point for the build scaffold, but the files are gone — re-derive/re-verify from spec. Choose freely.
- **Test framework** (GoogleTest/Catch2/hand-rolled) and CTest-vs-custom harness — freedom (`regression-suite-design.md:104-109`).
